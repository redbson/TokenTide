{
  "name": "tokentide-windows",
  "private": true,
  "description": "TokenTide's Windows tray app (Electron). Run it with `npm start`; scripts/build-windows.mjs builds the installer.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "main.mjs",
  "scripts": {
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "44.3.0",
    "electron-builder": "26.15.3"
  }
}
